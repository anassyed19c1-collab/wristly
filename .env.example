# Copy this file to .env.local for local dev, and set as environment
# variables on EC2 (via Terraform / user-data) for production.

# Format: mysql://USER:PASSWORD@HOST:3306/DBNAME
# Once RDS is created, this will point to the RDS endpoint.
DATABASE_URL="mysql://root:password@localhost:3306/wristly"
